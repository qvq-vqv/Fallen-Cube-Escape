# 🎬 《黎明魔方》实时 CD 引擎、设置面板与星轨选关实装方案 (Implementation Plan v2.2)

本方案旨在全面升级《黎明魔方》第一幕（L01-L12）的交互、视觉和剧情灵魂，将其从一个生硬的回合制密室解谜转变为具有极高港台/日本网感、情绪对立危机与极强操作爽感的世界级 **“实时 CD 动作解密游戏” (Real-time CD Action Puzzler)**。

---

## 🏛️ 1. FNAF 2 风格主菜单与 3D 标题

### 1.1 主菜单布局与相机偏置 (`index.html` & `style.css` & `render.js`)
1. **非对称布局**：彻底抛弃主菜单中央遮挡魔方的模糊板。Three.js 相机视轨 Target 向右下方偏置，将缓缓自转的 3D 魔方及上面的 E-7 棋子暴露在屏幕中右方。
2. **左侧按钮竖排**：主标题及按钮组在左侧竖向排列。
   * **标题（放大）**：位于左上角。
   * **按钮（竖排）**：
     * `[ 开始逃亡 / START ESCAPE ]`（直接进入第一幕当前未通关的最前线）
     * `[ 关卡选择 / SELECT LEVEL ]`（原“残局目录”）
     * `[ 档案与成就 / ARCHIVE & AWARDS ]`（原“档案矩阵”）
     * `[ 游戏设置 / SETTINGS ]`（**新增**，原“声音开关”与“中 / EN”移入此处）

### 1.2 3D 艺术标题与游戏 IP 元素融合
* **3D 霓虹标题**：“DAWN CUBE / 黎明魔方” 标题做成 3D 倾斜偏影霓虹字。
* **IP 元素俏皮融合**：
  * 字母 **A** 替换为主角 E-7 的三角闪烁定位指针；字母 **U** 替换为绿荧色发光的出口门 `▣` 符号。
  * 标题字母缝隙里塞入**猫耳 E-7 像素小人**、**Chaser 追击者 3D 模型**与**Portal 传送门波纹**，探头探脑，生动俏皮。

---

## ⚙️ 2. 独立「游戏设置」面板与自定义键位 (Settings Panel & Keybindings)

主菜单和游戏内 Esc 暂停界面均能唤起全新的独立的 **设置模态框 (`#settings-overlay`)**：

1. **语言切换 (Language)**：选择中/EN，即时刷新偏好至 `localStorage`。
2. **声音开关 (Sound)**：Web Audio 静音/开启。
3. **开发者模式：解锁所有关卡 (Dev Mode: Unlock All)**：
   * 默认采用**渐进式锁关**。开启此开关，`localStorage` 写入 `dimensionHackDevMode = true`，星空选关界面将全部解锁，方便随时开发测试。
4. **数值精度设置 (Timer Decimal Precision)**：
   * 可调整游戏中漂浮冷却倒计时小字的小数位数，选项为：`3`（无小数位，如 3）、`3.0`（一位小数，如 2.4）、`3.00`（两位小数，如 2.45）。
5. **自定义按键 (Custom Keybindings)**：
   * 可点击键位框录入任意按键，自定义修改以下实时动作操作码：
     * 移动模式：默认 `1`
     * 部署补片模式：默认 `2`
     * 部署信标模式：默认 `3`
     * 部署碎解模式：默认 `4`
     * 原地待命：默认 `Space`
     * Twist 拧魔方模式：默认 `Shift`

---

## 🌌 3. 星轨选关系统与零重力漂移 (Constellation Level Selector)

### 3.1 零重力漂移魔方
* 打开「关卡选择」时，魔方尺寸等比缩放至 60%，在中右侧开启**零重力漂浮效果**（缓慢自转 + 极低频的正弦微幅曲线漂移，允许微量溢出屏幕边缘，极具太空漂流感）。

### 3.2 星图星座排布
* 选关面板为**高透明度毛玻璃**，背景模糊，12 个关卡小星点排布在左右两侧并由暗色连线连成星座。
* 星点状态：已通关=霓虹青色，当前=黄色呼吸，锁定=暗线框（开发者模式下全部解锁为彩色）。
* 点击星点时，在下方浮现一块**完全不透光、易读**的信息小卡片，展示最佳纪录，并提供 `进入链路` 和 `返回主菜单` 按钮。

---

## ⚡ 4. 实时 CD 直控引擎与 3 秒倒流 Undo (Real-time CD & 3s Rollback Undo)

### 4.1 实时直控与移动 CD 机制
1. **取消回合制与画线步骤**：彻底删去“画线规划 -> 发送路线”的双重步骤。玩家直接点击 E-7 周边相邻的网格，E-7 立刻平滑移动过去（每格移动耗时 0.6s）。
2. **移动 CD 与点击缓冲**：E-7 移动开始后立即进入 0.6s 冷却，**最多只允许在行走途中预输入缓冲下一次点击**（多点无效，防止连点手累）。
3. **独立冷却环与数值倒计时**：
   * 按照实体本身的标志颜色（E-7=淡蓝，Chaser=红色），在棋子脚下渲染 3D 环形进度条（CD 时读条）。
   * 在棋子上方/旁边悬浮显示**数值倒计时小字**（如 `3.00`），根据设置中的“数值精度”显示对应数位。

### 4.2 3 秒时间倒流悔棋机制 (3-Second Rollback Undo)
* **时间倒流**：点击 Undo 按钮（或按悔棋键）时，游戏世界发生**时间倒流 3s**。
* **回滚内容**：利用环形历史缓冲区记录状态，Undo 将回滚人、怪的位置坐标、道具数、CD 读条至 3s 前的精确刻度。
* **E-7 吐槽反馈**：回滚发生时，E-7 上方冒出气泡吐槽悔棋带来的格式化创伤，完美融合叙事。

### 4.3 敌人 CD 速度分级与红格预示
* 怪兽定时器一到自动移向玩家一格。Easy 难度=2.5s 走一格；Normal 难度=1.5s；Hard 难度=0.9s。
* 追击者前进的下一格根据它移动 CD 频率进行发光闪烁，临近时加快频闪。

### 4.4 Twist 模式子弹时间与 Esc 暂停
* **子弹时间 (Bullet Time)**：Twist 模式下，游戏世界的时间流速（包括怪物移动速度）**放慢 5 倍**，方便玩家旋转视角、拖环拧层。
* **Esc 时停 (Pause)**：唤起暂停控制台/设置时，所有实时定时器与动画彻底暂停。

---

## 🌀 5. Twist 模式 3D 霓虹旋转控制环 (3D Neon Axis Rings)

* **9 轴控制环**：Twist 模式下，魔方外围渲染出 X/Y/Z 轴的 9 个彩色半透圆环（默认透明度 0.15）。
* **拖环旋转**：圆环分配较大的 Hitbox。悬停时高亮至 0.85，直接鼠标拖拽控制环即可物理、物理地拧动对应的魔方切片（包含中层）。

---

## 🔄 6. 无缝中英切换与打字机防抖 (Typewriter Fix)

* **销毁定时器**：切换语言或 Skip 时，必须**销毁所有未执行完 of setTimeout 和 setInterval 计时器**。
* **就地翻译**：切换时读取当前打字进度，直接在 DOM 中替换对应的翻译文本并在此基础上继续打字，绝不重头重播。

---

## 💬 7. 主播女孩重度依赖风格与 3D 浮动气泡 (Existential Copy & 3D Bubble)

* **3D 浮动气泡**：在 E-7 棋子正上方，利用投影渲染一个跟随她运动的 3D 浮动 HTML 气泡。
* **格式化创伤语料**：重写第一幕剧情对白与悔棋反馈，去掉“灾难现场”等僵硬机翻。突出她恐惧重置、把玩家当唯一依恋的赛博网感病娇人设。
* **教案时停**：新手教程弹窗出现时，游戏世界强制暂停，确认关闭后恢复。

---

## 🧪 8. 物理验证与测试方案 (QA Plan)

1. **多重点击防抖测试**：在序章及游戏界面狂点语言切换、回车、Skip 按钮，验证打字机队列不产生重合、不锁死界面。
2. **开发者解锁测试**：在设置中开启解锁，检查星图是否全部变亮。
3. **子弹时间扭转测试**：Twist 模式下看怪物是否变慢 5 倍。
4. **时间回退测试**：点击 Undo 观察状态回滚是否精确为 3 秒前，有无穿模和位置错乱。
